# Vue 3 Практика 27-28 - Компоненты, Роутинг и Генератор палитр

Полнофункциональное веб-приложение на Vue 3, демонстрирующее работу с компонентами, пропсами, v-model и Vue Router.

## Функциональность

### ✅ Практика 27: Генератор цветовых палитр

1. **Система генерации палитр**
   - Кнопка "Случайная палитра" генерирует гармоничные цветовые схемы
   - Отображение палитры в виде цветовых карточек
   - Показ HEX-значений для каждого цвета

2. **Управление отдельными цветами**
   - Клик по цветовой карточке копирует значение в буфер обмена
   - Всплывающее уведомление о успешном копировании
   - Возможность закрепить понравившийся цвет (кнопка 📍/📌)

3. **Базовые инструменты настройки**
   - Выбор количества цветов в палитре (3, 5, 7)
   - Переключение между форматами отображения (HEX, RGB)
   - Автоматическое сохранение текущей палитры в localStorage

4. **Просмотрщик палитры**
   - Превью палитры в mockup интерфейса (заголовок, карточка, кнопка)
   - Переключение светлого/тёмного фона для тестирования контраста

### ✅ Практика 28: Компоненты, пропсы, v-model и Vue Router

1. **Компоненты с пропсами**
   - `UserCard` - компонент карточки пользователя с валидацией пропсов
   - `UserCards` - список пользователей с фильтрацией и поиском
   - `SearchExample` - примеры использования v-model в формах

2. **Vue Router**
   - Навигация между страницами
   - Динамические маршруты с параметрами
   - Вложенные маршруты (nested routes)
   - Обработка 404 ошибок
   - Хуки навигации (beforeRouteEnter, beforeRouteUpdate)

3. **Views (Страницы)**
   - `HomePage` - главная страница с навигацией
   - `UserProfile` - профиль пользователя с вкладками
   - `UserSettings` - настройки с вложенными маршрутами
   - `NotFound` - страница 404

4. **Вложенные компоненты**
   - `ProfileInfo`, `ProfilePosts`, `ProfileFriends` - вкладки профиля
   - `SettingsGeneral`, `SettingsSecurity`, `SettingsNotifications` - вкладки настроек

## Технологии

- Vue 3 (Composition API + Options API)
- Vue Router 4
- Vite
- CSS3

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер разработки:
```bash
npm run dev
```

3. Откройте браузер по адресу: http://localhost:5173

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## Деплой на GitHub Pages

### Автоматический деплой (GitHub Actions)

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

1. **Настройте репозиторий:**
   - Убедитесь, что ваш репозиторий называется `frontend_backend_kr5`
   - Если название другое, обновите `base` в `vite.config.js`:
     ```js
     base: '/ваше-название-репозитория/'
     ```

2. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages
   - В разделе "Source" выберите "GitHub Actions"
   - Сохраните изменения

3. **Настройте права доступа:**
   - Settings → Actions → General
   - В разделе "Workflow permissions" выберите "Read and write permissions"
   - Сохраните изменения

4. **Загрузите код:**
   ```bash
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/frontend_backend_kr5.git
   git push -u origin main
   ```

5. **Автоматический деплой:**
   - После push в ветку `main` или `master` автоматически запустится workflow
   - Проверьте статус в разделе Actions вашего репозитория
   - После успешного деплоя приложение будет доступно по адресу:
     `https://ваш-username.github.io/frontend_backend_kr5/`

### Ручной деплой

Если нужно задеплоить вручную:

```bash
npm run build
```

Затем загрузите содержимое папки `dist/` в ветку `gh-pages` или используйте GitHub CLI:

```bash
gh-pages -d dist
```

### Важные замечания

- Убедитесь, что `base` в `vite.config.js` соответствует названию вашего репозитория
- Если репозиторий в подпапке организации, путь будет: `/организация/репозиторий/`
- Для корневого репозитория пользователя используйте: `base: '/'`

## Структура проекта

```
frontend_backend_kr5/
├── src/
│   ├── components/
│   │   ├── ColorPaletteGenerator.vue  # Генератор палитр (Практика 27)
│   │   ├── UserCard.vue              # Компонент карточки пользователя
│   │   ├── UserCards.vue             # Список пользователей с фильтрацией
│   │   └── SearchExample.vue         # Примеры v-model
│   ├── views/
│   │   ├── HomePage.vue              # Главная страница
│   │   ├── UserProfile.vue           # Профиль пользователя
│   │   │   ├── ProfileInfo.vue       # Вкладка информации
│   │   │   ├── ProfilePosts.vue      # Вкладка постов
│   │   │   └── ProfileFriends.vue    # Вкладка друзей
│   │   ├── UserSettings.vue          # Настройки
│   │   │   ├── SettingsGeneral.vue   # Основные настройки
│   │   │   ├── SettingsSecurity.vue  # Безопасность
│   │   │   └── SettingsNotifications.vue # Уведомления
│   │   └── NotFound.vue              # Страница 404
│   ├── router/
│   │   └── index.js                  # Конфигурация Vue Router
│   ├── App.vue                       # Главный компонент приложения
│   ├── main.js                       # Точка входа
│   └── style.css                     # Глобальные стили
├── index.html                        # HTML шаблон
├── vite.config.js                    # Конфигурация Vite
└── package.json                      # Зависимости проекта
```

## Использование

### Генератор палитр (Практика 27)
1. Перейдите на страницу "Палитры"
2. Нажмите кнопку "🎨 Случайная палитра" для генерации новой палитры
3. Кликните на цветовую карточку, чтобы скопировать её значение
4. Используйте кнопку 📍 для закрепления цвета
5. Выберите количество цветов (3, 5 или 7)
6. Переключите формат отображения между HEX и RGB
7. Все изменения автоматически сохраняются в localStorage

### Компоненты и роутинг (Практика 28)
1. Используйте навигацию в шапке для перехода между страницами
2. На странице "Пользователи" - фильтруйте и ищите пользователей
3. На странице "Поиск" - изучите примеры работы с v-model
4. В "Профиле" - переключайтесь между вкладками (Информация, Посты, Друзья)
5. В "Настройках" - переключайтесь между вкладками (Основные, Безопасность, Уведомления)
6. Попробуйте перейти на несуществующую страницу - увидите 404

## Критерии успеха

### Практика 27:
- ✅ Приложение генерирует случайные палитры
- ✅ Цвета отображаются с HEX-значениями
- ✅ Работает копирование в буфер обмена
- ✅ Палитра сохраняется между перезагрузками
- ✅ Интерфейс интуитивно понятен

### Практика 28:
- ✅ Компоненты используют пропсы с валидацией
- ✅ Реализована работа с v-model в формах
- ✅ Настроен Vue Router с динамическими маршрутами
- ✅ Реализованы вложенные маршруты (nested routes)
- ✅ Работает навигация между страницами
- ✅ Обработка 404 ошибок

